# Claim-Agent 개선 플랜 (2026-09)

작성 기준: `main` 최신 커밋 `4f0e3da` 시점의 코드·문서·테스트 전수 검토. 테스트는 `python -m pytest`로 110 passed, 1 skipped 확인.

이 문서는 요청 유형 `META`(프로젝트 설정·구조 감사)의 산출물이며, 청구항 파이프라인 에이전트는 호출하지 않았다.

---

## 0. 요약

- 파이프라인의 결정론 부기(게이트 계약, revision 해시, blind 격리, 텔레메트리·RCA)는 잘 갖춰져 있고 테스트 110개가 통과한다.
- 가장 큰 공백은 **품질 기준 데이터의 부재**다. eval fixture가 전부 합성 응답이라 9개 역할 프롬프트의 판정이 맞는지 자동으로 확인할 수 없다. CI도 없다.
- 사용자 체감이 큰 공백은 웹 후속 수정이 항상 전체 재설계로 실행되는 점, 실행 예산 상한 부재, PDF·HWP 미지원, 내보내기 부재다.
- 캐시·전송 점검(§1.3) 결과, 소스 캐시는 단일 호출에도 생성되어 단일 run에서는 비용·왕복만 늘리고, 원자료·도면은 모든 호출에 inline으로 재전송되며, 스타일 도구 단계와 웹 라우터가 고정 오버헤드를 만든다. 실측 지연 데이터는 저장소에 없다.
- 아래 P0(기반) → P1(체감·정확도) → P2(구조) 순으로 진행한다. 계약(`CLAUDE.md`)을 바꾸는 항목은 사용자 승인 뒤 별도 문서로 진행한다.

---

## 1. 현황 진단

### 1.1 잘 되어 있는 것

| 영역 | 근거 |
|---|---|
| 게이트 전제조건·단계 순서를 LLM이 아니라 Python이 결정 | `claim_copa/models/contracts.py`, `pipeline/transitions.py` |
| exact 문언 sha256 스탬프로 1자 변경 시 revision 강제 무효화 | `pipeline/claimtext.py`, `engine._new_revision` |
| blind 격리를 데이터 구조로 강제(금지 마커 스캔) | `pipeline/blind_guard.py` |
| 봉투 JSON ↔ 한국어 보고서 상호 대조, 복구 1회 | `pipeline/verify.py`, `engine._generate_validated` |
| 텔레메트리·RCA·선제 경고·승인제 교훈 루프 | `improve/feedback.py`, `improve/rca.py`, `improve/lessons.py` |
| 루프백 전용 웹 서버, 쿠키·Origin 검증, `.env` 업로드 거부 | `web.py` |
| 상태기계·무효화·격리·라우팅 단위 테스트 110개 | `tests/` |

### 1.2 검증된 공백

| # | 공백 | 근거 (파일·위치) | 영향 |
|---|---|---|---|
| G1 | **eval fixture가 전부 합성 응답**이다. 역할 보고서 본문이 "역할 보고서 본문 역할 보고서 본문…"으로 채워져 있다. | `eval/cases/sample-clip-holder/fixtures/*.json`, `scripts/make_sample_fixtures.py`, `docs/python_runtime.md` "현재 fixtures는 … 합성 응답" | 9개 역할 프롬프트(약 50KB)의 **품질을 재는 자동 신호가 0**이다. 테스트는 상태기계만 검증한다. |
| G2 | **CI가 없다.** `.github/` 부재, ruff·mypy 설정 없음. | 저장소 루트, `pyproject.toml` | 테스트가 있어도 자동으로 돌지 않는다. |
| G3 | **역할 파일 파생본이 드리프트**했다. `.claude/agents/*.md`가 단일 출처라고 문서화되어 있지만 `.codex/agents/*.toml` 9개 모두 본문이 불일치(CRLF·이스케이프 차이 포함, `claim-architect`는 길이 자체가 다름). 생성 스크립트가 저장소에 없고 `.ps1`은 Windows 전용. | `.codex/agents/`, `scripts/*.ps1` | 같은 역할이 Claude Code·Codex·웹 번들에서 다르게 동작할 수 있다. |
| G4 | **웹 후속 수정이 항상 ARCHITECT 재설계**로 실행된다. 띄어쓰기 한 곳을 고쳐도 d+1, r+1로 전체 8단계(+종속항 7단계·항별 2n회)를 다시 호출한다. CLI에는 `--apply-style-fix`, `--apply-meaning-fix`가 있으나 웹 경로는 이를 쓰지 않는다. | `conversation_pipeline.run_pipeline`: `Decision(restart_from="ARCHITECT", request_update=req)`; `engine.resume`는 `request_update` 시 ARCHITECT만 허용 | 사용자가 가장 자주 하는 행동(문언 미세 수정)이 가장 비싸다. |
| G5 | **실행 예산 가드가 없다.** `max_return_loops`만 있고 토큰·비용·호출 수 상한이 없다. `telemetry.pricing`이 비어 있어 비용 추정이 항상 0. | `config.PipelineConfig`, `claim-agent.yaml`, `store/telemetry.estimate_cost` | 루프 한도 내에서도 한 run이 얼마가 들지 실행 전에 알 수 없다. |
| G6 | **입력 형식이 텍스트·이미지뿐**이다. PDF·DOCX·HWP·HWPX 미지원을 사용법에 명시. | `models/request.TEXT_EXT`, `tui_support.validate_attachment`, `사용법.md` | 한국 실무 원자료(명세서·선행문헌)는 대부분 HWP/PDF다. 매번 수동 변환이 필요하다. |
| G7 | **선행기술(조건 6) 전용 단계가 없다.** `prior_art`는 모든 역할 패킷에 원자료로 첨부될 뿐, 문헌별 구성대비표·N/I/S 라벨을 만드는 역할·봉투 필드가 없다. `INVENTIVE_STEP` 게이트는 종속항 설계자에만 있다. | `pipeline/packets._materials`, `roles/whitelist.py`, `models/contracts.py` | FINALIZATION에서 선행기술을 줘도 조건 6·8의 근거표가 산출물로 남지 않는다. |
| G8 | **청구항 파서가 `【청구항 N】` 헤더만 인식**하고 부모항 판정이 "있어서" 위치 휴리스틱에 의존한다. 다중 종속은 기본값(`expand_multi_dependent: false`)에서 BLOCK. | `pipeline/claimtext._CLAIM_HEAD`, `_parents_of` | 사용자가 붙여 넣는 다양한 표기(`[청구항 1]`, `청구항 1.`)와 "제1항 또는 제2항에 있어서"가 종속항 단계에서 중지를 일으킨다. |
| G9 | **출력 내보내기가 없다.** 결과는 `report.md`와 `--claims-only` 텍스트뿐. | `store/report.py`, `cli.cmd_runs` | 명세서·심의자료로 옮길 때 복사·붙여넣기에 의존한다. |
| G10 | **리비전 간 변경 대조가 리포트에 없다.** `CLAUDE.md`는 변경 제안을 `기존 문언 / 제안 문언 / 변경 이유 / 권리범위 영향 / 근거`로 분리하라고 요구하지만, `RevisionState.history`와 `change_goal`이 저장만 되고 렌더되지 않는다. | `models/state.RevisionState`, `store/report.py` | 사용자가 r1→r2에서 무엇이 왜 바뀌었는지 `calls/`를 열어 봐야 한다. |
| G11 | **프로바이더가 Gemini 단일**이며 대화·라우팅 경로는 SDK를 직접 호출한다. 역할 파일 frontmatter는 `model: opus/fable`인데 Python 런타임은 이를 쓸 수 없다. | `chat.generate_turn`(google.genai 직접), `chat.routed_turn`(GeminiProvider 직접 생성), `provider/` | 모델 간 비교·전환이 불가능하고 대화 경로는 replay·record 대상이 아니다. |
| G12 | **저장소 위생.** 실제 발명 청구항 파일이 루트에 있고 eval이 `../../../`로 참조. `docs/python_runtime.md`는 "44 tests"라고 적혀 있으나 실제 111개. 실행기(`.cmd`, `.ps1`)가 Windows 전용. `claim_agent` 패키지는 `claim_copa.__path__` 재수출 방식. | `claim_agent_updated_claims_2026-08-27.md`, `eval/cases/reagent-tube-review-only/request.yaml`, `docs/python_runtime.md:157`, `claim_agent/__init__.py` | 신규 참여자 혼란, 기밀 원자료 노출 위험. |
| G13 | **로컬 감사 기록에 원자료 전문이 평문 저장**된다(`calls/*.json`의 `packet_text`). 보관·삭제 정책이 없다. | `engine._call` `call_payload["packet_text"]` | `sources/README.md`가 "원자료는 기밀일 수 있다"고 명시하지만 런타임에 보관 정책이 없다. |
| G14 | **웹 UI에 진행 상태·재개 수단이 없다.** 게이트 표, 단계 스텝퍼, 중지 시 재개(결정 입력·종류 선택), 비용 표시가 없다. | `web_assets/app.js`(103줄) | 멈춘 이유와 다음 행동을 알려면 `report.md`와 CLI가 필요하다. |

### 1.3 캐시·전송·응답 속도 점검 (2026-09-15)

점검 방법: `provider/cache.py`, `provider/gemini.py`, `pipeline/packets.py`, `engine._call` 정독 + 샘플 케이스 오프라인 리플레이(`runs/audit-01`)로 호출별 전송량 측정. 저장소에 실측 지연·토큰 텔레메트리가 없어 속도는 구조로만 판단했다.

**측정값 (문자 수)**

| 항목 | 값 |
|---|---|
| 역할별 시스템 지시 | 4.4K ~ 9.5K |
| 역할×scope 사전 로딩 소스 블록 | 독립항 15.6K ~ 36.8K, 종속항 21.2K ~ 58.0K (dependent success reviewer 최대) |
| 전체 run 호출 수 (독립항 + 종속항 3개) | 20회 (+ 스타일 도구 단계 2회, 복구 시 추가) |
| 원자료가 inline으로 재전송되는 호출 | 독립항 7회, 종속항 6회, 목표항별 picture n회 (blind 제외) |
| 웹 라우터 고정 입력 | `CLAUDE.md` 18.4K + 라우터 지시 1.7K + 문맥 최대 80K, 매 턴, 캐시 없음 |

**판정**

| # | 결과 | 근거 | 영향 |
|---|---|---|---|
| G15 | **소스 캐시가 단일 호출에도 생성된다.** `CacheManager.get_or_create`는 살아 있는 항목이 없으면 무조건 새 캐시를 만든다. 설정의 `cache.min_expected_reuse: 2`는 코드 어디에서도 읽지 않는다. 적중 시 TTL 연장(`caches.update`)도 없다. | `provider/cache.py`, `config.CacheConfig`, `grep min_expected_reuse` | 캐시는 생성 시 일반 입력 단가로 과금되고 저장 과금이 붙으며, 적중 시에만 할인된다. 한 run에서 역할×scope는 대부분 1회만 호출되므로 단일 run은 **생성 왕복 14회 + 소스 토큰 이중 과금 + 저장**만 추가되고 이득은 1시간 내 다른 run·루프·스타일 2단계·종속항 picture 팬아웃에서만 발생한다. 사람이 검토하다 1시간을 넘기면 재개 시 전부 재생성된다. |
| G16 | **원자료·도면이 모든 호출에 inline 재전송된다.** 이미지는 항상 `Part.from_bytes`, Files API·리사이즈·형식 변환 없음. 종속항 picture 팬아웃만 공통 블록을 캐시한다. | `provider/gemini._build_contents`, `packets._materials`, `packets.picture_dependent`만 `cache_text` 설정 | 수 MB 도면 × 13+n회 업로드가 지연을 만들고 요청 크기 제한(inline 합계 20MB)에 걸릴 수 있다. 큰 이미지는 타일 수에 비례해 토큰이 늘어 호출마다 반복 과금된다. Gemini는 캐시 사용 요청에 별도 `system_instruction`을 허용하지 않아(현재 구현도 캐시 안에 시스템 지시를 넣음) 역할 간 공유 캐시는 구조적으로 불가능하다. |
| G17 | **암묵 캐시 접두 일치가 깨지는 패킷 배치.** 소스 블록 → 패킷 순서인데 패킷 맨 앞 `RUN_HEADER`에 `record_id`·revision이 있어 같은 역할의 반복 호출(루프·복구·스타일 2단계)에서도 접두가 매번 달라진다. | `packets._hdr`, `gemini._build_contents` | 암묵 캐시를 지원하는 모델에서 무료로 얻을 수 있는 원자료·상위 보고서 재사용을 놓친다. |
| G18 | **스타일 조정자 도구 단계가 항상 실행된다.** 07 §3 조건과 무관하게 전체 패킷·이미지·소스 블록을 포함한 phase A를 먼저 호출하고, 대부분 `SEARCH_DONE`으로 끝난다. 설정 주석의 `aux_mode: prefetch`는 미구현. | `engine._call` `use_tools` 분기, `config.RoleConfig.aux_mode` | run당 +2회 호출(독립항·종속항 스타일 단계)과 그만큼의 지연. |
| G19 | **웹 라우터·대화 경로가 캐시 없이 고정 입력을 매 턴 전송한다.** `classify_request`는 `use_cache=False`, `generate_turn`은 이력 전체·이미지 base64를 매 턴 재전송. | `routing.classify_request`, `chat.generate_turn` | 일반 대화에도 답변 전 라우팅 호출이 선행되어 체감 지연이 붙는다. |
| G20 | **실행당 성능 요약이 없다.** 텔레메트리는 호출별 토큰·지연·캐시 적중을 기록하지만 `report.md`에는 나오지 않고 `feedback` 집계로만 본다. | `store/report.py`, `store/telemetry.py` | 어느 단계가 느리고 캐시가 실제로 적중했는지 사용자가 볼 수 없다. |

**잘 되어 있는 것**: 캐시 키가 모델·역할·scope·시스템 지시·소스 해시에 결속되어 역할 파일·교훈·소스 변경 시 자동 무효화된다. blind는 캐시·소스 대상에서 제외된다. 종속항 picture 팬아웃은 공통 블록·이미지를 한 캐시로 묶고 병렬 목표항이 한 번의 생성을 공유한다. 캐시 실패·만료 시 inline 폴백이 있다. 스트리밍은 지연을 늘리지 않는다.

**속도의 구조적 상한**: 게이트 순서상 독립항 8회, 종속항 6회 + 목표항별 2회가 직렬이며, 각 호출이 thinking HIGH + 최대 32K 출력(JSON + 보고서 전문 중복)이다. 출력 생성이 직렬 지연의 대부분을 차지하므로 캐시로 줄일 수 있는 것은 입력 전송·과금 쪽이고, 지연의 큰 부분은 thinking 수준·보고서 길이 조정(골든 세트 필요, P0-1)과 순서 재검토(승인 필요, P1-7-h)에서만 나온다.

---

## 2. 우선순위 근거

### 왜 골든 평가 세트와 CI를 먼저 하는가

1. 역할 파일 드리프트 해소(G3), 웹 revision 분류(G4), 선행기술 단계(G7), 프로바이더 교체(G11), 교훈 주입·thinking_level 실험은 모두 *모델 출력이 바뀌는* 변경이다. 지금은 그 변경이 개선인지 퇴행인지 판정할 기준 데이터가 없다(G1). 기준이 있으면 각 변경을 "골든 세트에서 통과율·주골격 보존·USER_LOCK 보존이 유지되는가"로 같은 잣대에 놓고 비교할 수 있다.
2. `eval run --record/--replay`, `expected.yaml`, shadow 비교, RCA는 이미 코드로 존재한다. 빠진 것은 **데이터**(실제 발명과 기대 결과)와 **자동 실행**이다. 새 인프라를 짓는 일이 아니다.
3. 사용자가 특허 실무자이므로 실제 발명 자료와 기대 청구항을 며칠 안에 제공할 수 있다. 라이브 녹화 비용은 4건 × 1회 수준이다.
4. 한 번 만들면 이후 모든 PR·프롬프트 수정·모델 변경에 재사용된다.

### 왜 웹 revision 분류(G4)와 예산 가드(G5)가 그 다음인가

사용자가 가장 자주 하는 행동은 문언 미세 수정인데, 현재 웹 경로는 그것을 가장 비싼 경로(전체 재설계)로 실행한다. 비용 상한이 없어 실행 전에 얼마가 들지도 알 수 없다. 이 둘은 골든 세트가 있으면 회귀 없이 배포할 수 있는 작은 변경이다.

### 완료 판단 기준

- 골든 케이스 수 ≥ 4, 발명 유형 ≥ 3종.
- CI가 push마다 `pytest` + replay eval을 실행하고, 주 1회 live eval 결과가 `eval/results/`에 쌓인다.
- 이후 프롬프트·엔진 변경 PR은 골든 세트 결과 표를 첨부한다.

---

## 3. 개선 항목 (우선순위별)

각 항목: **문제 → 제안 → 산출물 → 검증 → 규모**(S: 1일 이내, M: 2~4일, L: 1주 이상). 항목 번호 옆 괄호는 §1.2의 공백 번호.

### P0 — 기반

#### P0-1. 골든 평가 세트 (G1)
- **제안**: `eval/cases/`에 실제 발명 3~4건 추가. 유형 분포: PHYSICAL 1, PROCESS 1, DATA_CONTROL 1, HYBRID 또는 COMPOSITION 1. 각 케이스에 `sources/`(원자료), `request.yaml`, `expected.yaml`, 라이브 녹화 후 sanitize한 `fixtures/`.
- **`expected.yaml` 확장**(`improve/evalharness.evaluate`에 검사 추가):
  - `skeleton_terms`: 주골격 용어가 exact 문언에 모두 존재(SOURCE_EXACT_TERM 기준)
  - `forbidden_terms`: CONCEPT_LABEL_ONLY 등 즉석 조어 부재
  - `user_lock_preserved: true`
  - `expected_invention_type`: 설계자의 `invention_type.primary`
  - `max_calls`, `max_output_tokens_total`
  - `reconstruction: [PASS, PASS-RANGE]`
- **라이브 eval 명령 분리**: `claim-agent eval live --all`이 실행 전 예상 호출 수·비용(P1-2의 pricing 사용)을 출력하고 확인 후 실행. replay eval은 CI, live eval은 주 1회 수동.
- **검증**: `eval run --all --replay` 전 케이스 PASS; live 결과 표가 `eval/results/`에 생성.
- **규모**: M (코드) + 사용자 자료 제공 시간.

#### P0-2. CI와 정적 검사 (G2, G12 일부)
- **제안**: `.github/workflows/ci.yml` — Python 3.11/3.12 매트릭스에서 `pip install -e .[dev]`, `ruff check`, `mypy claim_copa`(점진 도입: `--ignore-missing-imports`, 오류 허용 목록), `pytest`, `claim-agent eval run --all` (replay), `claim-agent doctor --contracts`, 역할 파일 파생본 동기화 검사(P0-3). `pyproject.toml`에 `[tool.ruff]`, `[tool.mypy]` 추가. `docs/python_runtime.md`의 테스트 수 표기를 CI가 대조하거나 문구를 "전체 테스트"로 바꾼다.
- **검증**: PR에서 CI 상태 표시; 로컬 `ruff check .` 0건.
- **규모**: S~M.

#### P0-3. 역할 파일 단일 출처 빌드 (G3)
- **제안**: `scripts/build_role_derivatives.py`(Python, 크로스플랫폼)가 `.claude/agents/*.md`에서 frontmatter를 분리해 `.codex/agents/*.toml`과 웹 번들(`dist/claim-agent-web-<ver>/`)을 생성. `--check` 옵션으로 드리프트를 non-zero exit로 보고. 기존 `.ps1` 두 개는 이 스크립트를 호출하는 얇은 래퍼로 축소하거나 제거. 첫 실행으로 현재 드리프트를 일괄 해소.
- **검증**: `python scripts/build_role_derivatives.py --check`가 CI에서 0 exit; 9개 toml이 md 본문과 바이트 단위 일치(줄바꿈 LF 통일).
- **규모**: S.

### P1 — 사용자 체감 (비용·시간·입력·출력)

#### P1-1. 웹 후속 수정의 revision 종류 분류 (G4)
- **제안**:
  1. `routing.RouteDecision`에 `revision_kind: STYLE_ONLY | MEANING | DESIGN | NONE` 추가. 라우터 프롬프트에 판별 기준(조사·띄어쓰기·표면 용어만 → STYLE_ONLY; 절 결속·관계 술어 → MEANING; 주골격·계층·한정 집합·원자료 변경 → DESIGN) 명시.
  2. `conversation_pipeline.run_pipeline`에서 결정론 가드: 새 원자료·첨부·USER_LOCK 변화가 있으면 DESIGN 강제(`engine.resume`의 기존 제약과 일치). 그 외에는 `Decision(action=style_fix|meaning_fix|redesign)`으로 변환.
  3. 오분류 안전장치: STYLE_ONLY로 실행했는데 style adjuster가 `RETURN_TO_DRAFTER`를 반환하면 기존 루프 규칙대로 자동 상승(이미 구현됨).
  4. 웹 UI: 중지 상태 메시지 아래에 "재개" 패널(결정 문구 입력 + 종류 라디오 + 추가 자료 첨부 + 비게이팅 UNVERIFIED 수용).
- **검증**: `tests/test_routing.py`, `tests/test_web.py`에 STYLE_ONLY 후속이 architect·drafter를 호출하지 않고 r+1만 만드는 케이스; DESIGN 강제 케이스.
- **규모**: M.

#### P1-2. 실행 예산 가드와 비용 가시화 (G5)
- **제안**: `pipeline.max_calls`, `pipeline.max_total_tokens`, `pipeline.max_cost_usd`(선택) 설정 추가. `engine._loop`에서 누적 텔레메트리와 비교해 초과 시 `HALTED_BUDGET_LIMIT`(종료 코드 4 계열)로 중지하고 `resume`으로 한도 상향 후 재개 허용. `claim-agent.yaml`의 `telemetry.pricing`에 모델별 기본값 예시를 채우고 `doctor`가 configured 모델의 pricing 부재를 WARN. CLI `run` 시작 시와 웹 답변 하단에 "호출 N회 / 토큰 / 추정 비용" 표시.
- **검증**: scripted 테스트에서 `max_calls: 3`으로 4번째 호출 전 중지; `report.md`에 사유 기재.
- **규모**: S~M.

#### P1-3. 입력 형식 확장: PDF·DOCX·HWPX·HWP (G6)
- **제안**: `claim_copa/sources/extract.py`(신규)에 확장자별 텍스트 추출기를 두고 `models/request._load_path`와 `tui_support.validate_attachment`가 공용으로 사용.
  - PDF: `pypdf` 텍스트 레이어; 텍스트가 거의 없으면(스캔본) 명시 거부("OCR 미지원, 텍스트 PDF 또는 이미지로 제공"). 도면 PDF는 `--drawing`으로 지정 시 `pypdfium2`로 페이지→PNG 변환(선택 의존성).
  - DOCX: `python-docx` 문단·표 추출.
  - HWPX: zip 내 `Contents/section*.xml` 파싱(표준 라이브러리만).
  - HWP 5.0: `olefile` + zlib로 `BodyText/Section*` 텍스트 레코드 추출(선택 의존성, 실패 시 HWPX 변환 안내).
  - 추출 결과는 원본 sha256과 함께 `MaterialItem`에 저장하고, 원본 형식·추출기 버전을 `material_meta`에 기록해 `input_revision`에 반영.
- **검증**: 각 형식 최소 샘플 1개로 추출 단위 테스트; 웹 업로드 허용 목록 갱신; `사용법.md` 갱신.
- **규모**: M~L (HWP 5.0 바이너리 처리가 가장 큼; 1차는 PDF·DOCX·HWPX, 2차 HWP).

#### P1-4. 결과 내보내기 (G9)
- **제안**: `claim-agent export <run_id> --format docx|md [--with-evidence]`. 【청구항 N】 형식 본문, 상단에 LOCK 종류·revision·`명세서 뒷받침·실시가능성 미검증 잠정안` 표시, 선택적으로 근거표·게이트 표 부록. 웹 답변에 "DOCX 내보내기" 버튼(`/api/export`).
- **검증**: 생성 파일을 다시 읽어 exact 문언 sha256이 LOCK의 값과 일치.
- **규모**: S~M.

#### P1-7. 캐시·전송 효율 (G15~G20)
- **a. 캐시 생성 조건(`min_expected_reuse`) 구현** — 생성 조건: 레지스트리에 살아 있는 항목이 있거나(재사용), 이번 run에서 같은 역할×scope 호출이 N회 이상 예정(스타일 2단계, DEPENDENT_SINGLE picture n≥2, eval `--all`)이거나, `cache.warm: always`. 적중 시 `caches.update`로 TTL을 연장해 사람 검토 중 만료를 막는다. 검증: scripted 테스트에서 단일 호출 역할은 캐시 생성 0회, picture 팬아웃은 1회. 규모 S.
- **b. 도면 1회 정규화 + Files API** — 업로드 시 최대 변 2048px로 축소(도면 부호 판독을 위해 그 이하로 줄이지 않음), 원본·정규화본 sha256을 `material_meta`에 기록해 `input_revision`에 반영. run 시작 시 Files API로 1회 업로드하고 이후 호출은 URI 참조(캐시 콘텐츠에도 사용). blind 배제 규칙 유지. 검증: 호출별 `context_transport`에 업로드 바이트 0 확인. 규모 M.
- **c. 패킷 배치 재정렬** — 안정 블록(USER_LOCK·원자료·상위 보고서 전문)을 앞에, 가변 블록(`RUN_HEADER`·요청·출력 계약)을 뒤에 두어 암묵 캐시 접두 일치를 살린다. 역할 파일이 헤더 위치를 요구하지 않음을 확인한 뒤 적용. 검증: 루프·복구 호출의 텔레메트리 `cached_tokens` > 0. 규모 S.
- **d. (실험, 승인 필요) 역할 본문의 위치 이동** — 시스템 지시를 공통 어댑터 서문만으로 두고 역할 본문을 콘텐츠 첫 블록으로 옮기면 역할 간에도 접두(원자료·상위 보고서)를 공유할 수 있다. 지시 준수도가 바뀔 수 있으므로 골든 세트(P0-1) 품질 불변을 확인한 경우에만 채택. 규모 M.
- **e. 스타일 도구 단계 경량화** — phase A에서 이미지·`04_청구항_스타일가이드` 제외, thinking LOW, `max_output_tokens` 축소. 07 §3 판단은 그대로 모델이 한다. 규모 S.
- **f. 라우터·대화 캐시** — `ROUTER_SYSTEM + CLAUDE.md`를 explicit 캐시(TTL 1h)로 두고, 대화 경로를 `LLMProvider` 경유로 옮겨 같은 캐시·기록 규칙을 적용(P2-2와 연동). 규모 S.
- **g. 실행당 성능 요약** — `report.md`와 웹 답변 하단에 호출 수, 단계별·총 지연, 입력·캐시·출력 토큰, 캐시 적중률, 추정 비용을 표로 출력(P1-2와 병합). 규모 S.
- **h. (선택, 승인 필요) blind snapshot 투기 실행** — STYLE이 exact 문언을 봉인한 직후 blind 호출을 시작하고, OA_DRAFT_GATE PASS가 확인된 뒤에만 그 snapshot을 사용·기록한다. 상류 실패로 revision이 바뀌면 폐기한다. `CLAUDE.md`는 OA 이후 blind를 요구하므로 문구 해석 또는 계약 갱신에 대한 사용자 결정이 필요하다. 예상 이득은 직렬 1단계 지연. 규모 S.

### P1 — 검수 정확도

#### P1-5. 청구항 파서 강화 (G8)
- **제안**: `pipeline/claimtext.py`
  - 헤더 인식: `【청구항 N】`, `[청구항 N]`, `청구항 N.`, `청구항 N`(줄 단독), `Claim N`; 파싱 후 canonical `【청구항 N】`으로 정규화하고 정규화 여부를 기록.
  - 부모항 판정: "에 있어서" 앞 구간에서 `제N항`, `제N항 또는 제M항`, `제N항 내지 제M항 중 어느 한 항`, `제N항 및 제M항`을 명시 규칙으로 파싱해 `parent_nos` 확정. 휴리스틱(80/120자)은 폴백으로만.
  - 다중 종속 기본 정책 재검토: `expand_multi_dependent`를 `false`로 두되 BLOCK 대신 REVIEW로 낮추고 "대안 체인 n개 확장 실행" 재개 옵션을 안내.
- **검증**: 표기 변형 20개 이상, 다중 종속 5개 이상의 파라미터화 테스트.
- **규모**: S~M.

#### P1-6. 구조화 근거표 (G10 일부, 조건 4 자동 검사)
- **제안**: `RoleEnvelope`에 `limitation_evidence: [{limitation, role_FECNIS, source_name, location, basis: DIRECT|DERIVED|UNCONFIRMED, note}]`(flat, closed enum) 추가. 설계자·success reviewer가 채우고, 엔진이 "basis=UNCONFIRMED 0건"을 결정론 검사로 추가(위반 시 REVIEW). 리포트·export에 표로 렌더.
- **검증**: 스키마 depth ≤ 3 유지 테스트(`test_envelope_schema_is_flat_and_closed`); scripted 케이스에서 UNCONFIRMED 1건이 REVIEW로 중지.
- **규모**: M (역할 파일 출력 형식 갱신 포함 → P0-3 빌드 재실행).

### P2 — 구조·유지보수

#### P2-1. 리비전 diff 리포트 (G10)
- **제안**: `claim-agent runs diff <run_id> [--from r1 --to r2]`와 `report.md`의 "리비전 이력" 섹션. 형식은 `CLAUDE.md`의 `기존 문언 / 제안 문언 / 변경 이유 / 권리범위 영향 / 근거`. 변경 이유는 `RevisionState.change_goal`, 권리범위 영향은 해당 revision의 style record 변경 대조표 요약을 인용, 문언 차이는 문장 단위 diff.
- **규모**: S.

#### P2-2. 프로바이더 추상화 완성과 Anthropic 프로바이더 (G11)
- **제안**: (1) `chat.generate_turn`·`routed_turn`이 `LLMProvider`만 사용하도록 정리(스트리밍 콜백을 `CallSpec`/provider 인터페이스에 추가). (2) `provider/anthropic.py`: system prompt·prompt caching(`cache_control`)·JSON 출력(tool 강제 또는 스키마 프롬프트)·확장 사고 매핑. `thinking_level`은 프로바이더별 예산 표로 변환. (3) `claim-agent.yaml`에 `provider: gemini|anthropic`과 역할별 override. (4) 골든 세트로 두 프로바이더 결과를 `eval compare`.
- **주의**: 역할 프롬프트는 프로바이더 중립이어야 하므로 `adapter_preamble_ko.md`에 프로바이더별 문구가 들어가지 않게 유지.
- **규모**: L. P0-1이 없으면 착수하지 않는다.

#### P2-3. 선행기술 조건 6 전용 단계 (G7)
- **제안**: `prior_art_present`일 때만 ARCHITECT 뒤·DRAFT 전에 `prior-art-threat-reviewer` 단계를 조건부 삽입. 산출물은 성공조건 조건 6의 표(`한정 / 문헌 / 개시 상태 / 원문 근거 / 단독 신규성 영향 / 결합 후 영향 / N·I·S 후보`)를 봉투의 구조화 필드로. 설계자의 조건 8 배치 판정 입력으로 전달하고, FINALIZATION에서 `INVENTIVE_STEP`이 `UNVERIFIED`에서 벗어날 수 있는 유일한 경로로 둔다. 등록 가능성 단정 금지 문구 유지.
- **주의**: 역할 추가는 `CLAUDE.md`·`AGENTS.md`·`sources/README.md`·화이트리스트·계약의 동시 변경이며 `source_set_id`가 바뀐다. **사용자 승인 후** 별도 design 문서로 진행.
- **규모**: L.

#### P2-4. 웹 UI 진행 상태 (G14)
- **제안**: 단계 스텝퍼(독립항 8단계, 종속항 8단계)와 게이트 표 렌더(`state.json` 기반 `/api/state`), 재개 패널(P1-1), 비용 표시(P1-2), 리비전 diff 탭(P2-1), 내보내기 버튼(P1-4). 현재 markdown 렌더러 유지, HTML 주입 금지 원칙 유지.
- **규모**: M.

#### P2-5. 저장소 정리 (G12)
- **제안**:
  - `claim_agent_updated_claims_2026-08-27.md` → `eval/cases/reagent-tube-review-only/sources/claims.md`, `request.yaml` 경로 수정. 실제 발명 자료라면 공개 저장소 적합성 확인.
  - `설정_개선보고서.md` → `docs/history/2026-08-26_draft-final-lock-분리.md`.
  - `docs/python_runtime.md`의 "44 tests" 갱신(P0-2와 연동).
  - `claim_copa` → `claim_agent` 실제 개명, `claim_copa/__init__.py`는 호환 shim으로 유지. 저장된 명령·리포트의 `claim-copa` 별칭 유지.
  - `claim-agent web` 서브커맨드 추가(`scripts/launch_web.py`를 감싸기)로 Linux/macOS에서도 더블클릭 없이 동일 진입.
  - `doctor`가 configured 모델이 `models` 목록에 없으면 가장 가까운 이름을 제안.
- **규모**: S.

#### P2-6. 감사 기록 보관 정책 (G13)
- **제안**: `claim-agent runs purge --older-than 30d [--keep-locks]`, `.tui/requests` 동일. `claim-agent.yaml`에 `retention.days`. 문서에 "runs/·.tui/는 원자료 전문을 포함하므로 공유 폴더·클라우드 동기화 대상에서 제외"를 명시. (암호화는 범위 밖; OS 디스크 암호화 안내로 대체.)
- **규모**: S.

---

## 4. 실행 순서

| 단계 | 기간 | 항목 | 완료 기준 |
|---|---|---|---|
| 0 | 1~2일 | P0-2 CI 골격, P0-3 역할 파일 빌드·동기화, P2-5 저장소 정리 | CI 녹색, 드리프트 0, 루트에 발명 자료 없음 |
| 1 | 1~2주 | **P0-1 골든 세트**(사용자 자료 필요), P1-2 예산 가드 + P1-7-g 성능 요약, P1-7-a·c·e 캐시 생성 조건·패킷 배치·도구 단계 경량화, P1-1 웹 revision 분류 | 케이스 ≥ 4, live 기준선 기록, 단일 호출 역할의 캐시 생성 0회, STYLE_ONLY 후속이 2회 호출로 끝남 |
| 2 | 2~4주 | P1-3 입력 형식(PDF·DOCX·HWPX 먼저), P1-7-b 도면 정규화·Files API, P1-7-f 라우터 캐시, P1-4 내보내기, P1-5 파서, P2-1 diff, P2-4 웹 UI | 각 항목 테스트 + 골든 세트 replay 통과 |
| 3 | 검토 후 | P1-6 근거표, P1-7-d 역할 본문 위치 실험, P1-7-h blind 투기 실행, P2-3 선행기술 단계, P2-2 Anthropic 프로바이더, P2-6 보관 정책 | 사용자 승인된 계약 변경 문서 + 골든 세트 live 비교표 |

단계 1의 P0-1은 코드보다 **사용자 자료**가 병목이다. 코드 작업(expected 확장, live 명령)은 단계 0 직후 바로 시작할 수 있다.

---

## 5. 하지 않을 것

- 게이트 의존 단계(success → syntax → OA → blind → picture)의 병렬화. `CLAUDE.md`가 순서를 요구하며, 종속항 역구성만 이미 팬아웃되어 있다.
- lessons 자동 승인. 승인제는 유지한다.
- LLM 판정(예: LLM-as-judge)으로 결정론 게이트를 대체하는 것. 골든 세트 평가에서도 LLM 채점은 보조 지표로만 쓴다.
- 정식 명세서 없이 `FINAL_CLAIM_LOCK`을 만드는 경로 추가.
- 선행기술 없이 신규성·진보성 `PASS`를 내는 어떤 변경.

---

## 6. 사용자에게 필요한 것

1. 골든 세트용 실제 발명 3~4건: 발명 설명(텍스트), 도면(이미지), 있으면 선행기술 1~2건, 그리고 "정답으로 인정할 독립항 1항"과 종속항 2~3항. 기밀이면 저장소 외부 경로(`eval/private/`, `.gitignore`)로 두고 fixture만 sanitize해 커밋한다.
2. `telemetry.pricing`에 넣을 사용 모델의 단가(또는 확인 위임).
3. P2-3(선행기술 단계)처럼 `CLAUDE.md` 계약을 바꾸는 항목의 착수 승인.
4. P1-7-d(역할 본문 위치)와 P1-7-h(blind 투기 실행)의 실험 착수 여부 결정. 둘 다 골든 세트로 품질 불변을 확인한 뒤에만 기본값으로 바꾼다.
5. 캐시·속도 실측을 위해 `claim-agent doctor --live`로 사용 모델의 캐시·암묵 캐시 지원을 확인하고, 실제 run 1회의 `telemetry.jsonl`을 공유(원자료는 담기지 않는다).

---

## 7. 진행 상태 (2026-09-15 실행분)

브랜치 `claude/confident-fermi-0a4812`. 아래 표의 "완료"는 코드·테스트·문서가 커밋된 상태를 뜻하며, 실제 모델 호출은 이 환경에 API 키가 없어 fixtures·가짜 클라이언트로만 검증했다.

| 항목 | 상태 | 비고 |
|---|---|---|
| P0-1 골든 평가 세트 | 코드 완료 / **데이터 미완** | `expected.yaml` 확장, `eval new`, `eval live`(비용 미리보기), `eval/cases/README.md`. 실제 발명 3~4건·기대 청구항·라이브 녹화는 사용자 자료 필요 |
| P0-2 CI·정적 검사 | 완료 | `.github/workflows/ci.yml`, ruff 0건, mypy는 권고(41건) |
| P0-3 역할 파일 단일 출처 빌드 | 완료 | `scripts/build_role_derivatives.py`, 9개 TOML 재생성, CI `--check` |
| P1-1 웹 revision 경로 | 완료 | `revision_kind` + 결정론 가드, `/api/resume`, 종속항 전용 범위 |
| P1-2 예산 가드·비용 | 완료 | `max_calls/max_total_tokens/max_cost_usd`, 성능 요약, 단가 예시 |
| P1-3 입력 형식 | 완료 | PDF·DOCX·HWPX·HWP 5.0; 스캔 PDF·암호화 거부 |
| P1-4 내보내기 | 완료 | `export` DOCX/MD + 재검증, `/api/export` |
| P1-5 파서 강화 | 완료 | 헤더 표기 5종, 부모항 명시 규칙, 다중 종속 REVIEW + `--expand-multi` |
| P1-6 구조화 근거표 | 완료 | `limitation_evidence`, PASS+UNCONFIRMED → REVIEW, 어댑터 서문 v2 |
| P1-7 a·c·e·f·g 캐시·전송 | 완료 | 생성 정책·TTL 연장·패킷 재배치·도구 단계 경량화·라우터 캐시·성능 요약 |
| P1-7-b 도면·Files API | 완료 | 2048px 정규화, 해시당 1회 업로드, inline 폴백 |
| P1-7-d 역할 본문 위치 실험 | **미착수(승인 필요)** | 골든 세트 확보 후 품질 불변 확인이 전제 |
| P1-7-h blind 투기 실행 | **미착수(승인 필요)** | `CLAUDE.md` 순서 해석이 걸림 |
| P2-1 리비전 diff | 완료 | `runs diff`, report.md 리비전 이력, 웹 대조 보기 |
| P2-2 프로바이더·Anthropic | 완료(라이브 미검증) | `provider.kind`, `AnthropicProvider`, 대화 경로 프로바이더 경유 |
| P2-3 선행기술 조건 6 단계 | **미착수(승인 필요)** | `CLAUDE.md`·역할·화이트리스트 계약 변경 |
| P2-4 웹 UI | 완료 | 스텝퍼·게이트 표·중지 사유·재개 패널·비용·내보내기·diff |
| P2-5 저장소 정리 | 완료 | 파일 이동, `claim_copa→claim_agent`(shim), `claim-agent web`, doctor 제안 |
| P2-6 보관 정책 | 완료 | `runs purge`, `retention.*`, 삭제 로그 |
