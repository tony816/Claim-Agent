# Claim-Agent 사용법

특허를 다루지만 프로그램은 익숙하지 않은 분을 위한 안내다. 명령은 그대로 복사해서 붙여 넣으면 된다.

## 마우스로 실행하기 (웹 채팅)

프로젝트 폴더의 **`Claim-Agent.cmd`** 또는 바탕화면의 **`Claim-Agent 실행`**을 더블클릭하면 기본 브라우저에서 대화창이 열린다. IDE나 명령 입력은 필요 없다. 기존 `.env`의 API 키와 `gemini-3.8-flash` 설정을 그대로 사용한다.

- **대화:** 아래 입력창에 한글로 입력하고 Enter 또는 화살표 버튼으로 보낸다. Shift+Enter는 줄바꿈이다. 한글 조합을 확정하는 Enter는 메시지를 전송하지 않는다.
- **이어서 질문·수정:** 같은 입력창에 다음 질문이나 “더 짧게 수정해 줘” 같은 피드백을 입력한다. 질문과 답변이 위로 쌓인다.
- **첨부:** 입력창의 **＋** 버튼, 파일 드래그, 이미지 복사 후 Ctrl+V를 사용한다. 하나의 첨부 목록에서 ×로 보내기 전에 취소할 수 있다. 지원 형식은 UTF-8 TXT·MD·JSON·YAML·CSV, PDF(텍스트 PDF)·DOCX·HWPX·HWP와 PNG·JPG·WEBP, 파일당 20MB까지다. 스캔 PDF는 읽지 못한다.
- **작업 상태 패널:** 청구항 작업이 끝나거나 멈추면 대화 아래에 단계 스텝퍼, 게이트 표, 중지 사유, 호출 수·토큰·추정 비용이 표시된다. 같은 패널에서 **DOCX/MD 내보내기**, **리비전 대조**(기존 문언 / 제안 문언 / 변경 이유 / 권리범위 영향 / 근거)를 볼 수 있다.
- **재개·수정:** 패널의 재개 칸에 결정 내용을 적고 종류를 고른다. 멈춘 작업은 *같은 단계 재실행* 또는 *미검증 수용*(명세서·선행기술 부재만), 완료된 작업은 *스타일만*(조사·띄어쓰기, r+1) / *의미*(절 결속·관계 술어, r+1) / *재설계*(주골격·한정 집합, d+1)를 고른다. 자료를 새로 첨부하면 규칙상 항상 재설계로 실행된다. 대화창에 "띄어쓰기만 고쳐줘"처럼 적어도 자동 분류가 같은 경로를 고른다.
- **자동 분류:** 기본 **자동 분류 · 대화**에서도 청구항 출력물·수정안을 요구하면 작성 파이프라인으로, 특허 의견을 요구하면 관련 리뷰어로 자동 연결한다. 설정·로그 질문과 일반 대화는 구분한다. 분류나 필수 게이트가 실패하면 미검증 상태를 표시하고 일반 답변으로 우회하지 않는다.
- **웹챗에서 의견 묻기:** **청구항 작성·수정**을 선택해 둔 상태에서도 “어떤 방법을 택해야 하는가”처럼 판단 근거를 물으면 의견 검토로 처리한다. 종속항 체크박스가 켜져 있어도 의견 요청에 종속항 작성을 추가하지 않는다. 실제 수정본이 필요하면 “수정한 청구항을 작성해줘”라고 요청한다.
- **청구항 작성:** **청구항 작성·수정**을 직접 선택할 수도 있다. 앞서 이 대화에 입력한 사용자 설명과 첨부도 원자료로 연결된다. **작성 옵션**에서 종속항 범위를 지정할 수 있다. 자동 경로에서는 요청 문장의 종속항 번호·범위를 판별한다.
- **청구항 수정:** 같은 대화에서 **청구항 작성·수정**으로 후속 요청을 보내면 기존 작업의 원자료와 USER_LOCK을 유지하고 설계부터 새 revision으로 검수한다. 다른 발명은 **새 대화**에서 시작한다.
- **실시간 로그:** 실행 중에는 에이전트 요청과 공개 응답이 표시된다. 완료하면 자동으로 접히며, 답변 위의 **작업 로그 펼치기**로 다시 확인한다. 도구 결과는 해당 호출 완료 후 표시된다. 전체 이벤트는 `.tui/requests/`에 보존되고 화면 로그는 최근 30만 글자까지 제공된다.
- **프로젝트:** 왼쪽 **프로젝트** 옆 **＋**를 누르면 지침·소스 파일을 미리 설정해 두는 프로젝트 폴더가 생긴다. 프로젝트 화면에서 이름·설명·**지침**(예: “종속항은 2~6항까지”, “청구항만 출력”)·선택적 **USER_LOCK 확정 문언**을 저장하고, **소스 파일**에 발명 자료·도면·선행기술·정식 명세서를 종류를 골라 등록한다. **이 프로젝트에서 새 대화**를 누르면 그 프로젝트에 속한 새 채팅이 열리며, 프로젝트를 고른 상태에서 **＋ 새 대화**를 눌러도 같다. 프로젝트에 속한 모든 대화는 매 요청마다 저장된 지침·확정 문언·소스 파일을 자동으로 함께 보내므로 대화마다 다시 첨부할 필요가 없다. 대화 위쪽의 프로젝트 표시를 누르면 설정 화면으로 돌아간다. 지침은 답변 방식에 대한 지시이며 발명의 기술 원자료로 취급되지 않는다. 프로젝트 파일을 바꾼 뒤의 후속 수정은 새 자료가 있는 경우처럼 재설계로 실행된다. **전체 대화**를 누르면 프로젝트에 속하지 않은 대화도 함께 보인다. 프로젝트를 삭제하면 지침·파일만 지워지고 대화는 남는다.
- **대화 기록:** 왼쪽 목록에서 이전 대화를 다시 연다. 새로고침하거나 프로그램을 다시 실행해도 대화가 유지된다. 작성 중인 텍스트는 같은 브라우저에 임시 보존된다. 보내기 전 첨부 목록은 새로고침 후 다시 선택한다.
- **중지·종료:** 입력창의 ■는 현재 응답을 중지한다. 브라우저 탭을 닫아도 진행 중인 작업은 계속된다. 왼쪽 아래 **프로그램 종료**는 로컬 서버와 진행 중인 작업을 종료한다.

화면은 이 컴퓨터의 브라우저에서 동작하고 모델 요청은 Gemini API로 전송된다. 대화·첨부 기록은 `.tui/web/`, 프로젝트 폴더는 `.tui/web/projects/`, 청구항 결과는 설정된 `runs/`에 저장된다. 정식 명세서나 선행기술을 별도로 지정한 최종 검증은 아래 CLI의 `--spec`, `--prior-art` 옵션을 사용한다.

## 이전 터미널 화면 사용하기 (선택)

기존 터미널 작업실은 `python -m claim_agent.tui`로 열 수 있다. 더블클릭 실행기의 기본 화면은 위의 웹 채팅이다. Python 3.11 이상이 필요하다.

1. 위쪽 모드에서 **자동 분류 · 대화** 또는 **청구항 잠정안 작성**을 고르고, **프롬프트 → 대화·요청·피드백** 칸에 원하는 내용을 적는다. 자동 분류에서는 청구항·특허 요청도 내용에 맞는 전문 경로로 연결된다.
2. **파일 선택** 버튼으로 자료를 넣는다. 탐색기에서 파일을 복사한 뒤 **붙여넣기** 버튼을 눌러도 된다. 스크린샷 등 클립보드 이미지도 PNG 도면으로 첨부된다.
3. 텍스트로 된 원자료는 **발명 설명 붙여넣기** 탭에 붙여 넣을 수 있다. 요청과 원자료는 별도로 저장된다.
4. 자료는 분류 선택 없이 **첨부 파일** 목록에 함께 넣는다. 잘못 넣은 파일은 목록에서 선택한 뒤 **선택 삭제**를 누른다.
5. 필요하면 아래로 스크롤해서 종속항 작성과 범위를 설정한 뒤 **실행**을 누른다. 기본 모델은 `gemini-3.8-flash`이며, 기존 `.env`의 API 키를 사용한다.
6. 오른쪽에서 응답과 결과를 확인한다. 실행 중에는 **실시간 로그**가 열리고, 에이전트에 전달한 요청과 반환되는 응답이 중계된다. 응답이 끝나면 로그는 자동으로 접힌다. **실시간 로그 펼치기**를 누르면 다시 볼 수 있다. **결과 복사**, **결과 폴더** 버튼으로 결과를 가져올 수 있다.

**자동 분류 · 대화:** 같은 창에서 앞선 질문·답변을 기억하며 이어서 대화한다. 청구항 출력물·수정안은 필수 작성·검수 단계로, 특허 의견은 요청된 검수 역할로 연결된다. 의견 검토만으로 LOCK을 발급하지 않는다. **새 대화**는 현재 대화 맥락과 입력·첨부 목록을 초기화하며, 저장된 파일은 지우지 않는다.

**피드백으로 수정:** 결과 옆의 **결과 수정**을 누르고 프롬프트에 수정할 내용을 적는다. 일반 대화의 답변은 이전 대화 맥락을 포함해 수정한다. 청구항 결과는 **기존 작업에 피드백 반영** 모드에서 수정할 작업을 선택하고 **수정 실행**을 누른다. 이전 창에서 만든 작업도 저장된 최근 50개 작업 목록에서 선택할 수 있다. 기존 원자료·USER_LOCK·종속항 요청을 유지하면서 설계부터 재검토하고, 새 revision에서 필수 검수와 LOCK을 다시 수행한다. 추가 첨부나 발명 설명은 새로운 원자료로 전달된다. 이전 PASS를 수정된 문언에 그대로 재사용하지 않으므로 피드백 반영에도 API 호출이 발생한다.

**실시간 로그:** 실제 API 요청과 반환된 출력만 표시한다. 도구를 사용하는 단계의 응답·도구 결과는 해당 호출이 완료된 시점에 표시된다. 여러 종속항을 병렬 처리하면 에이전트와 목표항을 표시해 구별한다. 로그를 접어도 내용은 유지되며, 전체 요청·응답 이벤트는 `.tui/requests/<실행 ID>/events.jsonl`에 저장된다. 화면에는 최근 로그를 최대 30만 글자까지 표시한다. 로그에는 제공한 원자료가 포함될 수 있다.

**파일 드래그:** 터미널이 파일 드롭을 경로 입력으로 전달하는 경우, 요청 칸에 끌어 넣으면 자동 첨부된다. 경로 입력 칸에 끌어 넣었다면 **추가**를 누른다. 여러 파일은 따옴표로 둘러싼 경로 또는 줄바꿈으로 구분할 수 있다. 터미널에 따라 드래그가 전달되지 않을 수 있으므로 그때는 파일 선택이나 탐색기 복사 → 붙여넣기 버튼을 사용한다. 폴더가 아닌 파일을 선택한다.

**텍스트 붙여넣기:** 입력 칸을 클릭하고 터미널의 `Ctrl+V` 또는 `Ctrl+Shift+V`를 사용한다. 입력 칸에 포커스를 둔 상태에서 `F3`을 눌러도 된다. 붙여넣기 버튼에서 일반 텍스트를 읽으면 발명 설명 탭에 들어간다. 결과 보고서는 **결과 복사** 버튼으로 복사한다.

지원 형식은 `.txt`, `.md`, `.yaml`, `.yml`, `.json`, `.csv`, `.pdf`(텍스트가 들어 있는 PDF), `.docx`, `.hwpx`, `.hwp`(한글 5.0)와 `.png`, `.jpg`, `.jpeg`, `.webp`다. 스캔한 PDF(글자가 이미지인 것)와 암호가 걸린 문서는 읽지 못하므로 텍스트 PDF·원본 문서 또는 도면 이미지로 준비한다. 큰 도면은 긴 변 2048px로 자동 축소해 보낸다. `.env`는 첨부할 수 없다.

통합 첨부에서는 텍스트를 발명 원자료로, 이미지를 도면으로 자동 전달한다. 파일명만 보고 정식 명세서나 선행기술로 추정하지 않는다. 선행기술을 별도 비교 자료로 지정하거나 정식 명세서에 근거한 최종 검증을 실행하려면 아래 CLI의 `--prior-art`, `--spec` 옵션을 사용한다.

단축키: `Ctrl+Enter` 실행, `F2` 파일 선택, `F3` 붙여넣기, `F6` 중지, `Ctrl+Q` 종료. 화면이 좁으면 창을 최대화하거나 왼쪽 패널을 스크롤한다.

중지는 로컬 실행 프로세스를 종료하며 저장된 기록을 유지한다. 이미 전송된 API 요청에는 사용량이 발생할 수 있다. 중간에 멈춘 작업의 재개는 아래 CLI 재개 명령을 사용한다. 붙여넣은 원자료와 이미지는 `.tui/`, 실행 결과는 설정된 `runs/`에 저장되며 Git에 포함되지 않는다.

터미널 명령으로 열 때는 `python -m claim_agent.cli tui` 또는 `python -m claim_agent.tui`를 사용할 수도 있다.

---

## 1. 이게 무엇인가

발명 설명과 도면을 주면 **한국어 특허 청구항 초안을 만들고, 아홉 명의 검수자가 차례로 검사한 뒤, 통과한 문언만 잠가 주는 프로그램**이다.

검수자는 이런 순서로 일한다.

```
설계자 → 초안 작성자 → 문체 조정자 → 성공조건 검수자 → 통사 검수자
      → OA 검수자 → 블라인드 복원자 → 기준 비교자 → 잠금(LOCK)
```

중요한 점 두 가지.

- **블라인드 복원자는 발명을 모른다.** 청구항 문언만 받아서 "이것만 읽고 도면을 그릴 수 있는가"를 확인한다. 그래서 작성자가 스스로 채점하는 일이 구조적으로 불가능하다.
- **한 글자라도 바뀌면 앞의 모든 검수가 무효가 된다.** 프로그램이 문언의 지문(해시)을 찍어 두고 자동으로 확인한다.

정식 명세서가 없어도 **잠정안**은 끝까지 만들 수 있다. 다만 결과에 `명세서 뒷받침·실시가능성 미검증 잠정안`이라는 표시가 붙고, 출원용 최종안으로는 승격되지 않는다.

---

## 2. 준비 (처음 한 번만)

### 2-1. 설치

터미널에서 이 폴더로 이동한 뒤:

```bash
pip install -e .
```

PDF·DOCX·HWP 읽기와 도면 축소에 필요한 구성요소(pypdf, python-docx, olefile, Pillow)도 함께 설치된다. 더블클릭 실행기는 빠진 구성요소가 있으면 처음 실행 때 스스로 설치한다.

Gemini 대신 Claude(Anthropic)로 돌리려면 `claim-agent.yaml`의 `provider.kind`를 `anthropic`으로 바꾸고 `.env`에 `ANTHROPIC_API_KEY`를 넣은 뒤 `pip install anthropic`을 한 번 실행한다. 절차·게이트·기록은 같다.

### 2-2. Gemini API 키 등록

[Google AI Studio](https://aistudio.google.com/apikey)에서 키를 받아 등록한다.

프로젝트 루트(`claim-agent.yaml`이 있는 폴더)의 `.env` 파일을 열어 다음과 같이 저장한다. 파일이 없으면 `.env.example`을 `.env`라는 이름으로 복사한다.

```dotenv
GEMINI_API_KEY=여기에_받은_키
```

이후 명령을 실행할 때마다 `.env`를 자동으로 읽으므로 터미널을 다시 열어도 키를 입력할 필요가 없다. 기존 설치에서는 `pip install -e .`를 다시 실행해 추가 의존성을 설치한다.

같은 이름의 터미널 환경변수가 이미 있으면 그 값이 우선한다. `GOOGLE_API_KEY`도 대체 키로 사용할 수 있고, 별도 변수명을 쓰려면 `claim-agent.yaml`의 `model.api_key_env`에 지정한다. `--project-root`를 지정하면 그 폴더의 `.env`를 읽는다. 실제 키가 담긴 `.env`는 Git에서 제외된다.

### 2-3. 제대로 붙었는지 확인

```bash
claim-agent doctor --live
```

`[OK]`가 죽 나오면 준비 끝이다. `[WARN]`이나 `[FAIL]`이 있으면 그 줄에 이유가 적혀 있다.

### 2-4. 모델 이름 맞추기 ★ 중요

설정 파일의 기본 모델 이름은 **아직 확인되지 않은 값**이다. 실제로 쓸 수 있는 이름을 확인해서 고쳐야 한다.

```bash
claim-agent models
```

목록이 나오면 그중 쓰려는 이름을 `claim-agent.yaml` 파일 맨 위 `default:` 뒤에 적는다.

```yaml
model:
  default: gemini-3.8-flash      # ← 이 줄을 목록에 있는 이름으로 교체
```

---

## 3. 처음 5분: 키 없이 동작만 보기

API 키가 없어도 미리 녹화해 둔 응답으로 전체 흐름을 볼 수 있다.

```bash
claim-agent run --request-yaml eval/cases/sample-clip-holder/request.yaml \
  --replay eval/cases/sample-clip-holder/fixtures --run-id 연습-01
```

끝나면 결과를 본다.

```bash
claim-agent runs report 연습-01
```

청구항만 보고 싶으면:

```bash
claim-agent runs report 연습-01 --claims-only
```

---

## 4. 내 발명으로 실행하기

### 4-1. 자료 준비

폴더 하나를 만들고 자료를 넣는다. 형식은 `.md` 또는 `.txt`, 도면은 `.png`나 `.jpg`.

```
내발명/
  발명설명.md      ← 구성, 작용, 효과를 아는 만큼 적는다
  도면1.png
  요청.md          ← 무엇을 만들어 달라는 것인지
```

`발명설명.md`에는 이런 것이 있으면 좋다. 없으면 없는 대로 둔다. **프로그램은 없는 내용을 지어내지 않고 `근거 미확인`으로 표시한다.**

- 각 부품의 이름과 생김새
- 부품끼리 어떻게 붙어 있고 어떻게 움직이는가
- 그래서 무슨 문제가 해결되는가

`요청.md` 예시:

```
케이블 클립 홀더의 독립항 1항 잠정안을 작성한다.
정식 명세서는 아직 없다. 선행기술은 제공하지 않는다.
```

### 4-2. 실행

**독립항만 만들기**

```bash
claim-agent run --request-file 내발명/요청.md \
  --source 내발명/발명설명.md \
  --drawing 내발명/도면1.png \
  --prior-art NONE
```

**독립항 + 종속항까지**

```bash
claim-agent run --request-file 내발명/요청.md \
  --source 내발명/발명설명.md \
  --drawing 내발명/도면1.png \
  --prior-art NONE \
  --dependent --dependent-target "2~8"
```

**이미 확정한 문언을 절대 바꾸지 않게 하기**

확정 문언을 파일에 넣고 `--user-lock-file`로 준다. 프로그램은 이 문언이 최종 결과에 글자 그대로 남아 있는지 확인하고, 사라졌으면 즉시 멈춘다.

```bash
claim-agent run --request-file 내발명/요청.md \
  --source 내발명/발명설명.md \
  --user-lock-file 내발명/확정문언.txt \
  --prior-art NONE
```

**정식 명세서가 있어서 출원용 최종안을 원할 때**

```bash
claim-agent run --mode FINALIZATION \
  --request-file 내발명/요청.md \
  --source 내발명/발명설명.md \
  --spec 내발명/명세서.md \
  --prior-art 내발명/선행문헌1.md
```

---

## 5. 결과 읽는 법

실행이 끝나면 화면에 한 줄이 뜬다.

```
[run-20260913-01] outcome=DRAFT_CLAIM_LOCK stage=DONE revision=r1 design=d1
  report: runs/run-20260913-01/report.md
```

`outcome`이 답이다.

| 표시 | 뜻 | 다음 할 일 |
|---|---|---|
| `DRAFT_CLAIM_LOCK` | 잠정 독립항 완성 | 결과를 읽는다 |
| `DRAFT_CLAIM_LOCK+DRAFT_DEPENDENT_SET_LOCK` | 잠정 독립항 + 종속항 완성 | 결과를 읽는다 |
| `FINAL_CLAIM_LOCK` | 출원용 최종안 완성 | 결과를 읽는다 |
| `HALTED_REVIEW` | **사람 판단이 필요해 멈춤** | 6장으로 |
| `HALTED_BLOCK` | 문언이나 근거에 결함이 있어 멈춤 | 6장으로 |
| `HALTED_LOOP_LIMIT` | 같은 지적이 반복되어 멈춤 | 6장으로 |

보고서는 항상 세 부분이다.

1. **최종안** — 청구항 전문
2. **핵심 판단** — 단계별로 어떤 검수자가 무엇을 통과시켰는지
3. **남은 REVIEW/BLOCK/UNVERIFIED** — 아직 확인되지 않은 것

`UNVERIFIED`가 보여도 대부분 정상이다. 자주 나오는 두 가지:

- `OA_FINAL_GATE: UNVERIFIED — SPEC_NOT_PROVIDED` → 정식 명세서를 안 줬으니 법적 뒷받침은 확인 못 했다는 뜻. 잠정 작성은 정상 진행된 것이다.
- `신규성·진보성: UNVERIFIED — PRIOR_ART_NOT_PROVIDED` → 선행기술을 안 줬으니 등록 가능성은 판단하지 않았다는 뜻.

---

## 6. 멈췄을 때

멈추면 보고서 맨 아래에 **무엇이 문제이고 어떤 명령으로 재개하는지**가 적혀 있다. 원인이 궁금하면:

```bash
claim-agent runs rca run-20260913-01
```

어디서 처음 틀어졌는지, 되돌아간 지점이 어디인지, 비슷한 일이 전에도 있었는지를 정리해 준다.

재개는 **결정의 성격**에 따라 명령이 다르다. 이게 핵심이다.

| 내가 하려는 것 | 명령 |
|---|---|
| 그냥 판단을 알려주고 같은 단계를 다시 | `claim-agent resume <run_id> --decide "오목면의 주체는 기둥 둘레면"` |
| 띄어쓰기·조사·용어만 고치기 | `claim-agent resume <run_id> --decide "길이 방향으로 띄어쓰기" --apply-style-fix` |
| 문장 구조나 기술관계 표현을 다시 쓰기 | `claim-agent resume <run_id> --decide "각각의 분배 원천을 명시" --apply-meaning-fix` |
| 발명 구조 자체를 다시 설계하기 | `claim-agent resume <run_id> --decide "날개부를 상위 구성으로" --redesign` |
| 자료를 더 주기 | `claim-agent resume <run_id> --add-source 내발명/도면2.png` |
| 미검증 상태를 그대로 받아들이기 | `claim-agent resume <run_id> --accept-unverified` |

> `--accept-unverified`는 명세서·선행기술 미제공에만 쓸 수 있다. 실제 결함(REVIEW·BLOCK)을 통과로 바꾸는 기능은 없다.

---

## 6-1. 예산과 비용

한 번에 너무 많이 호출되지 않도록 `claim-agent.yaml`의 `pipeline.max_calls`(기본 60)가 있다. 한도에 닿으면 `HALTED_BUDGET_LIMIT`으로 멈추고, `claim-agent resume <run_id> --max-calls 120`처럼 한도를 올려 이어서 진행한다. 토큰·달러 한도(`max_total_tokens`, `max_cost_usd`)도 둘 수 있다. 보고서 아래 **성능 요약** 표에 단계별 호출·시간·토큰·추정 비용이 나오며, 비용은 `telemetry.pricing`에 사용 모델의 단가를 적어야 계산된다.

다중 종속항("제1항 또는 제2항에 있어서")이 있으면 역구성을 대안 체인마다 따로 돌려야 하므로 `REVIEW`로 멈춘다. 확장 실행을 승인하려면 `claim-agent resume <run_id> --expand-multi`.

---

## 7. 이미 있는 청구항만 검토받기

문언을 만들지 않고 검사만 받고 싶을 때 쓴다. 잠금(LOCK)은 만들어지지 않는다.

```bash
claim-agent review --claim-file 내청구항.md \
  --reviewers syntax-scope-reviewer,oa-strategy-reviewer
```

고를 수 있는 검수자:

- `syntax-scope-reviewer` — 문장 구조, 귀속, 부모항 인용관계
- `oa-strategy-reviewer` — 거절이유 관점의 위험
- `claim-success-reviewer` — 성공조건 전반

---

## 7-1. 결과 내보내기와 리비전 대조

```bash
claim-agent export run-20260913-01 --format docx                 # 【청구항 N】 형식 + 잠금 정보 헤더 (runs/<run_id>/claims-….docx)
claim-agent export run-20260913-01 --format docx --with-evidence # 게이트 표·성능 요약·한정별 근거표·미검증 항목 부록
claim-agent runs diff run-20260913-01                            # 직전 리비전과 대조: 기존 문언 / 제안 문언 / 변경 이유 / 권리범위 영향 / 근거
claim-agent runs diff run-20260913-01 --from r1 --to r3 --scope DEPENDENT_SET
```

내보낸 DOCX는 프로그램이 다시 읽어 잠긴 문언이 그대로인지 확인한 뒤에만 저장한다. 웹에서는 작업 상태 패널의 버튼이 같은 일을 한다.

---

## 8. 쓸수록 좋아지게 하기 (선택)

써 본 기록이 쌓이면 반복되는 문제를 찾아볼 수 있다.

```bash
claim-agent feedback
```

- 어떤 검수자가 자주 막는지, 비용과 시간이 어디에 쓰이는지
- **선제 경고** — 최근 실행이 예전보다 나빠지고 있으면 미리 알려 준다

**골든 케이스 만들기(권장).** 실제 발명 한 건과 "이 정도면 맞다"는 기대 청구항을 넣어 두면, 이후 설정·프롬프트를 바꿀 때 좋아졌는지 같은 잣대로 비교할 수 있다.

```bash
claim-agent eval new 내발명-케이스                 # eval/cases/내발명-케이스/ 템플릿 생성 → 자료와 기대값 채우기
claim-agent eval live --case 내발명-케이스 --record eval/cases/내발명-케이스/fixtures   # 예상 비용을 보여 주고 확인 후 1회 실제 호출·녹화
claim-agent eval run --case 내발명-케이스          # 이후는 녹화본으로 무료 재검사
```

**오래된 기록 정리.** `runs/`와 `.tui/`에는 발명 원자료 전문이 남으므로 주기적으로 지운다. 잠금(LOCK)에 도달한 작업은 기본으로 남긴다.

```bash
claim-agent runs purge --older-than 30 --dry-run   # 지울 대상만 확인
claim-agent runs purge --older-than 30
```

반복되는 문제를 다음 실행의 주의사항으로 바꾸려면:

```bash
claim-agent lessons propose --from-feedback   # 초안 만들기
claim-agent lessons list                      # 초안 읽어 보기
claim-agent lessons approve L-0001            # 마음에 들면 승인
```

**승인하기 전에는 아무것도 반영되지 않는다.** 승인한 것만 다음 실행부터 검수자에게 "절차상 주의사항"으로 전달된다. 발명의 기술내용 근거로는 쓰이지 않는다.

마음에 안 들면 `claim-agent lessons reject L-0001`.

---

## 9. 자주 겪는 문제

**"GEMINI_API_KEY is not set"**
2-2로 돌아가 프로젝트 루트의 `.env`에 `GEMINI_API_KEY` 값이 저장되어 있는지 확인한다. `--project-root`를 사용했다면 해당 폴더의 `.env`를 확인한다.

**"models.list … NOT found"**
`claim-agent.yaml`의 모델 이름이 실제와 다르다. 2-4로.

**모든 실행이 `HALTED_BLOCK`으로 끝난다**
`claim-agent runs rca <run_id>`로 어디서 막히는지 본다. 대개 발명 설명에 그 한정의 근거가 없는 경우다. 프로그램은 없는 내용을 만들어 채우지 않는다.

**너무 느리거나 비싸다**
`claim-agent feedback`으로 어느 검수자가 토큰을 많이 쓰는지 본 뒤, `claim-agent.yaml`에서 그 역할의 `thinking_level`을 `HIGH` → `MEDIUM`으로 낮춰 본다. 바꾸기 전에 비교해 보려면:

```bash
claim-agent eval run --case sample-clip-holder \
  --variant experiments/example-medium-thinking.yaml --baseline
```

**결과 파일은 어디에 있나**
`runs/<run_id>/` 안에 전부 있다.

| 파일 | 내용 |
|---|---|
| `report.md` | 최종 보고서 |
| `rca.md` | 원인 분석 (실행했을 때만) |
| `records/` | 단계별 기록과 잠금(LOCK) |
| `calls/` | 각 검수자에게 보낸 내용과 받은 답 전문 |
| `telemetry.jsonl` | 호출별 통계 (청구항 원문은 들어가지 않는다) |

---

## 10. 알아 둘 한계

- **등록 가능성을 판정하지 않는다.** 선행기술을 주지 않으면 신규성·진보성은 계속 `UNVERIFIED`다.
- **정식 명세서 없이는 출원용 최종안이 나오지 않는다.** 잠정안까지만 만들어진다.
- **기본 모델 이름은 확인된 값이 아니다.** 2-4를 반드시 거친다.
- **스캔 PDF는 읽지 못한다.** OCR을 지원하지 않으므로 텍스트 PDF·DOCX·HWP 원본이나 도면 이미지로 준비한다.
- **기록에는 원자료가 평문으로 남는다.** `runs/`·`.tui/`를 공유 폴더나 클라우드 동기화 대상에 두지 말고 `claim-agent runs purge`로 정리한다.
- **법률적 최종 판단은 사람이 한다.** 이 프로그램은 문언과 근거의 정합성을 검사할 뿐이다.

---

자세한 설정과 내부 구조는 [`docs/python_runtime.md`](docs/python_runtime.md)에 있다.
