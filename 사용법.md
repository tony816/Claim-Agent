# Claim Copa 사용법

특허를 다루지만 프로그램은 익숙하지 않은 분을 위한 안내다. 명령은 그대로 복사해서 붙여 넣으면 된다.

---

## 1. 이게 무엇인가

발명 설명과 도면을 주면 **한국어 특허 청구항 초안을 만들고, 아홉 명의 검수자가 차례로 검사한 뒤, 통과한 문언만 잠가 주는 프로그램**이다.

검수자는 이런 순서로 일한다.

```
설계자 → 초안 작성자 → 문체 조정자 → 성공조건 검수자 → 통사 검수자
      → OA 검수자 → 블라인드 복원자 → 기준 비교자 → 잠금(LOCK)
```

중요한 점 두 가지.

- **블라인드 복원자는 발명을 모른다.** 청구항 문언만 받아서 "이것만 읽고 도면을 그릴 수 있는가"를 확인한다. 그래서 작성자가 스스로 채점하는 일이 구조적으로 불가능하다.
- **한 글자라도 바뀌면 앞의 모든 검수가 무효가 된다.** 프로그램이 문언의 지문(해시)을 찍어 두고 자동으로 확인한다.

정식 명세서가 없어도 **잠정안**은 끝까지 만들 수 있다. 다만 결과에 `명세서 뒷받침·실시가능성 미검증 잠정안`이라는 표시가 붙고, 출원용 최종안으로는 승격되지 않는다.

---

## 2. 준비 (처음 한 번만)

### 2-1. 설치

터미널에서 이 폴더로 이동한 뒤:

```bash
pip install -e .
```

### 2-2. Gemini API 키 등록

[Google AI Studio](https://aistudio.google.com/apikey)에서 키를 받아 등록한다.

```bash
# macOS / Linux
export GEMINI_API_KEY="여기에_받은_키"

# Windows PowerShell
$env:GEMINI_API_KEY="여기에_받은_키"
```

터미널을 닫으면 사라진다. 매번 치기 싫으면 `~/.zshrc`(macOS) 또는 PowerShell 프로필에 같은 줄을 넣어 둔다.

### 2-3. 제대로 붙었는지 확인

```bash
claim-copa doctor --live
```

`[OK]`가 죽 나오면 준비 끝이다. `[WARN]`이나 `[FAIL]`이 있으면 그 줄에 이유가 적혀 있다.

### 2-4. 모델 이름 맞추기 ★ 중요

설정 파일의 기본 모델 이름은 **아직 확인되지 않은 값**이다. 실제로 쓸 수 있는 이름을 확인해서 고쳐야 한다.

```bash
claim-copa models
```

목록이 나오면 그중 쓰려는 이름을 `claim-copa.yaml` 파일 맨 위 `default:` 뒤에 적는다.

```yaml
model:
  default: gemini-3.8-flash      # ← 이 줄을 목록에 있는 이름으로 교체
```

---

## 3. 처음 5분: 키 없이 동작만 보기

API 키가 없어도 미리 녹화해 둔 응답으로 전체 흐름을 볼 수 있다.

```bash
claim-copa run --request-yaml eval/cases/sample-clip-holder/request.yaml \
  --replay eval/cases/sample-clip-holder/fixtures --run-id 연습-01
```

끝나면 결과를 본다.

```bash
claim-copa runs report 연습-01
```

청구항만 보고 싶으면:

```bash
claim-copa runs report 연습-01 --claims-only
```

---

## 4. 내 발명으로 실행하기

### 4-1. 자료 준비

폴더 하나를 만들고 자료를 넣는다. 형식은 `.md` 또는 `.txt`, 도면은 `.png`나 `.jpg`.

```
내발명/
  발명설명.md      ← 구성, 작용, 효과를 아는 만큼 적는다
  도면1.png
  요청.md          ← 무엇을 만들어 달라는 것인지
```

`발명설명.md`에는 이런 것이 있으면 좋다. 없으면 없는 대로 둔다. **프로그램은 없는 내용을 지어내지 않고 `근거 미확인`으로 표시한다.**

- 각 부품의 이름과 생김새
- 부품끼리 어떻게 붙어 있고 어떻게 움직이는가
- 그래서 무슨 문제가 해결되는가

`요청.md` 예시:

```
케이블 클립 홀더의 독립항 1항 잠정안을 작성한다.
정식 명세서는 아직 없다. 선행기술은 제공하지 않는다.
```

### 4-2. 실행

**독립항만 만들기**

```bash
claim-copa run --request-file 내발명/요청.md \
  --source 내발명/발명설명.md \
  --drawing 내발명/도면1.png \
  --prior-art NONE
```

**독립항 + 종속항까지**

```bash
claim-copa run --request-file 내발명/요청.md \
  --source 내발명/발명설명.md \
  --drawing 내발명/도면1.png \
  --prior-art NONE \
  --dependent --dependent-target "2~8"
```

**이미 확정한 문언을 절대 바꾸지 않게 하기**

확정 문언을 파일에 넣고 `--user-lock-file`로 준다. 프로그램은 이 문언이 최종 결과에 글자 그대로 남아 있는지 확인하고, 사라졌으면 즉시 멈춘다.

```bash
claim-copa run --request-file 내발명/요청.md \
  --source 내발명/발명설명.md \
  --user-lock-file 내발명/확정문언.txt \
  --prior-art NONE
```

**정식 명세서가 있어서 출원용 최종안을 원할 때**

```bash
claim-copa run --mode FINALIZATION \
  --request-file 내발명/요청.md \
  --source 내발명/발명설명.md \
  --spec 내발명/명세서.md \
  --prior-art 내발명/선행문헌1.md
```

---

## 5. 결과 읽는 법

실행이 끝나면 화면에 한 줄이 뜬다.

```
[run-20260913-01] outcome=DRAFT_CLAIM_LOCK stage=DONE revision=r1 design=d1
  report: runs/run-20260913-01/report.md
```

`outcome`이 답이다.

| 표시 | 뜻 | 다음 할 일 |
|---|---|---|
| `DRAFT_CLAIM_LOCK` | 잠정 독립항 완성 | 결과를 읽는다 |
| `DRAFT_CLAIM_LOCK+DRAFT_DEPENDENT_SET_LOCK` | 잠정 독립항 + 종속항 완성 | 결과를 읽는다 |
| `FINAL_CLAIM_LOCK` | 출원용 최종안 완성 | 결과를 읽는다 |
| `HALTED_REVIEW` | **사람 판단이 필요해 멈춤** | 6장으로 |
| `HALTED_BLOCK` | 문언이나 근거에 결함이 있어 멈춤 | 6장으로 |
| `HALTED_LOOP_LIMIT` | 같은 지적이 반복되어 멈춤 | 6장으로 |

보고서는 항상 세 부분이다.

1. **최종안** — 청구항 전문
2. **핵심 판단** — 단계별로 어떤 검수자가 무엇을 통과시켰는지
3. **남은 REVIEW/BLOCK/UNVERIFIED** — 아직 확인되지 않은 것

`UNVERIFIED`가 보여도 대부분 정상이다. 자주 나오는 두 가지:

- `OA_FINAL_GATE: UNVERIFIED — SPEC_NOT_PROVIDED` → 정식 명세서를 안 줬으니 법적 뒷받침은 확인 못 했다는 뜻. 잠정 작성은 정상 진행된 것이다.
- `신규성·진보성: UNVERIFIED — PRIOR_ART_NOT_PROVIDED` → 선행기술을 안 줬으니 등록 가능성은 판단하지 않았다는 뜻.

---

## 6. 멈췄을 때

멈추면 보고서 맨 아래에 **무엇이 문제이고 어떤 명령으로 재개하는지**가 적혀 있다. 원인이 궁금하면:

```bash
claim-copa runs rca run-20260913-01
```

어디서 처음 틀어졌는지, 되돌아간 지점이 어디인지, 비슷한 일이 전에도 있었는지를 정리해 준다.

재개는 **결정의 성격**에 따라 명령이 다르다. 이게 핵심이다.

| 내가 하려는 것 | 명령 |
|---|---|
| 그냥 판단을 알려주고 같은 단계를 다시 | `claim-copa resume <run_id> --decide "오목면의 주체는 기둥 둘레면"` |
| 띄어쓰기·조사·용어만 고치기 | `claim-copa resume <run_id> --decide "길이 방향으로 띄어쓰기" --apply-style-fix` |
| 문장 구조나 기술관계 표현을 다시 쓰기 | `claim-copa resume <run_id> --decide "각각의 분배 원천을 명시" --apply-meaning-fix` |
| 발명 구조 자체를 다시 설계하기 | `claim-copa resume <run_id> --decide "날개부를 상위 구성으로" --redesign` |
| 자료를 더 주기 | `claim-copa resume <run_id> --add-source 내발명/도면2.png` |
| 미검증 상태를 그대로 받아들이기 | `claim-copa resume <run_id> --accept-unverified` |

> `--accept-unverified`는 명세서·선행기술 미제공에만 쓸 수 있다. 실제 결함(REVIEW·BLOCK)을 통과로 바꾸는 기능은 없다.

---

## 7. 이미 있는 청구항만 검토받기

문언을 만들지 않고 검사만 받고 싶을 때 쓴다. 잠금(LOCK)은 만들어지지 않는다.

```bash
claim-copa review --claim-file 내청구항.md \
  --reviewers syntax-scope-reviewer,oa-strategy-reviewer
```

고를 수 있는 검수자:

- `syntax-scope-reviewer` — 문장 구조, 귀속, 부모항 인용관계
- `oa-strategy-reviewer` — 거절이유 관점의 위험
- `claim-success-reviewer` — 성공조건 전반

---

## 8. 쓸수록 좋아지게 하기 (선택)

써 본 기록이 쌓이면 반복되는 문제를 찾아볼 수 있다.

```bash
claim-copa feedback
```

- 어떤 검수자가 자주 막는지, 비용과 시간이 어디에 쓰이는지
- **선제 경고** — 최근 실행이 예전보다 나빠지고 있으면 미리 알려 준다

반복되는 문제를 다음 실행의 주의사항으로 바꾸려면:

```bash
claim-copa lessons propose --from-feedback   # 초안 만들기
claim-copa lessons list                      # 초안 읽어 보기
claim-copa lessons approve L-0001            # 마음에 들면 승인
```

**승인하기 전에는 아무것도 반영되지 않는다.** 승인한 것만 다음 실행부터 검수자에게 "절차상 주의사항"으로 전달된다. 발명의 기술내용 근거로는 쓰이지 않는다.

마음에 안 들면 `claim-copa lessons reject L-0001`.

---

## 9. 자주 겪는 문제

**"GEMINI_API_KEY is not set"**
2-2로 돌아가 키를 등록한다. 터미널을 새로 열었으면 다시 해야 한다.

**"models.list … NOT found"**
`claim-copa.yaml`의 모델 이름이 실제와 다르다. 2-4로.

**모든 실행이 `HALTED_BLOCK`으로 끝난다**
`claim-copa runs rca <run_id>`로 어디서 막히는지 본다. 대개 발명 설명에 그 한정의 근거가 없는 경우다. 프로그램은 없는 내용을 만들어 채우지 않는다.

**너무 느리거나 비싸다**
`claim-copa feedback`으로 어느 검수자가 토큰을 많이 쓰는지 본 뒤, `claim-copa.yaml`에서 그 역할의 `thinking_level`을 `HIGH` → `MEDIUM`으로 낮춰 본다. 바꾸기 전에 비교해 보려면:

```bash
claim-copa eval run --case sample-clip-holder \
  --variant experiments/example-medium-thinking.yaml --baseline
```

**결과 파일은 어디에 있나**
`runs/<run_id>/` 안에 전부 있다.

| 파일 | 내용 |
|---|---|
| `report.md` | 최종 보고서 |
| `rca.md` | 원인 분석 (실행했을 때만) |
| `records/` | 단계별 기록과 잠금(LOCK) |
| `calls/` | 각 검수자에게 보낸 내용과 받은 답 전문 |
| `telemetry.jsonl` | 호출별 통계 (청구항 원문은 들어가지 않는다) |

---

## 10. 알아 둘 한계

- **등록 가능성을 판정하지 않는다.** 선행기술을 주지 않으면 신규성·진보성은 계속 `UNVERIFIED`다.
- **정식 명세서 없이는 출원용 최종안이 나오지 않는다.** 잠정안까지만 만들어진다.
- **기본 모델 이름은 확인된 값이 아니다.** 2-4를 반드시 거친다.
- **법률적 최종 판단은 사람이 한다.** 이 프로그램은 문언과 근거의 정합성을 검사할 뿐이다.

---

자세한 설정과 내부 구조는 [`docs/python_runtime.md`](docs/python_runtime.md)에 있다.
